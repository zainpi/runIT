# Local coding-agent tool only. Put real values in ignored
# .env.higgsfield.local or export them into the agent's shell.
# Never use NEXT_PUBLIC_ for either credential.
HF_API_KEY_ID=
HF_API_KEY_SECRET=
